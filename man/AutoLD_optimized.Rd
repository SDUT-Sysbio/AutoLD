% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main_wrapper.R
\name{AutoLD_optimized}
\alias{AutoLD_optimized}
\title{Optimized Parallel Pipeline for Chromosome-wide LD Estimation}
\usage{
AutoLD_optimized(
  chr_name,
  geno_df,
  info_df,
  method = "full",
  window_kb = 200,
  n_cores = 1,
  ploidy = 4
)
}
\arguments{
\item{chr_name}{Character. The name of the chromosome to analyze (e.g., "LR999451.1").}

\item{geno_df}{A data frame or matrix of genotype dosages. Rows must represent markers (SNPs)
and columns must represent samples. Values should be integer dosages corresponding to the
ploidy level (e.g., 0, 1, 2, 3, 4 for tetraploids) or \code{NA} for missing data.
The number of rows must exactly match the number of rows in \code{info_df}.}

\item{info_df}{A data frame containing variant information. It must include at least two
mandatory columns: \code{CHROM} (chromosome ID) and \code{POS} (physical position in base pairs).
The row order must strictly align with \code{geno_df}.}

\item{method}{Character. The resolution model: \code{"full"} or \code{"partial"}. Default is \code{"full"}.}

\item{window_kb}{Numeric. The sliding window size in kilobases (kb). Default is 200.}

\item{n_cores}{Integer. Number of CPU cores for parallel processing. Default is 1.}

\item{ploidy}{Integer. The ploidy level (e.g., 4 or 6). Default is 4.}
}
\value{
A data frame containing pairwise LD estimations, p-values, and standardized statistics.
}
\description{
Optimized Parallel Pipeline for Chromosome-wide LD Estimation
}
